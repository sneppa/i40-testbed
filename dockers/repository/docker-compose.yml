version: "2"
services:
  repodb:
    container_name: repodb
    image: mongo
    volumes:
      - /data/db
      - /data/configdb
    ports:
      - "27018:27018"
  repository:
    container_name: repository
#    restart: always
    build: ./
    ports:
      - "8090:8090"
      - "30000-30100:30000-30100"
    links:
      - repodb
    depends_on:
      - repodb